#include <stdio.h>

/**
 * main - prints the name of the file it compiles from
 * followed by a new line
 * Return: 0 if successful
*/

int main(void)
{
    printf("%s\n", __FILE__);
    return (0);
}

