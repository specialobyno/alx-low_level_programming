#include <stdio.h>

/**
 * main: function will print "Programming is like a multilingual puzzle
 * Return: 0 if code run successfully, otherwise return non-zero
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
